# פרומפט לשיחה חדשה — העתק והדבק

> העתק את כל הטקסט שמתחת לקו, וצרף לשיחה החדשה את הקבצים:
> `extract_licenses.py`, `classifications.csv`, ואת קובץ הפלט האחרון `licenses.xlsx`
> (או לפחות את גיליון `תוצרים-DISTINCT` כ-CSV).

---

## רקע

אני עובד על פרויקט חילוץ נתונים מרישיונות יבואן רכב של משרד התחבורה.
יש לי סקריפט פייתון עובד (מצורף) שקורא קבצי `.docx` ומייצא לאקסל.
היקף: כ-2000 קבצים בסופו של דבר. אני מריץ על Windows, `C:\PY\Importers`.

## מה הסקריפט עושה כבר (עובד ונבדק על קבצים אמיתיים)

`python extract_licenses.py <תיקיית_docx> <פלט.xlsx>`

הסקריפט + `classifications.csv` יושבים באותה תיקייה.

מייצר קובץ אקסל עם 6 גיליונות:
`ישיר-חברות`, `ישיר-תוצרים`, `עקיף-חברות`, `עקיף-תוצרים`, `זעיר-חברות`, `תוצרים-DISTINCT`
(גיליונות התוצרים מקושרים לחברות לפי ח.פ — יחס אחד-לרבים).
בנוסף: `extraction.log` ו-`unknown_classifications.csv`.

מחלץ ברמת חברה: ח.פ / מספר עוסק מורשה, סוג ישות, שם חברה (כולל יחידים "למר X"),
סוג רישיון, תאריך מתן, תאריך תוקף, שם המנהל המאשר, תאריך עדכון נספח, ביטול, סטטוס OCR.
ברמת תוצר: תוצר, יצרן, סיווגים (מקור + יעד מתורגם), תאריך תוקף,
סוכן ראשי/משני + תאריך תוקף נפרד לכל אחד, תנאים והגבלות.

כבר מטופל: תאים ממוזגים ב-Word, סוכן משני בשורת המשך, טבלאות ללא כותרת,
סוגריים לא מאוזנים (`M1 M2))`), קודים דבוקים (`N1M2`), כותרות משתנות (סוכן מורשה / דילר).

**חשוב: הסקריפט עובד. אל תשכתב אותו מאפס. רק שינויים ממוקדים.**

## שתי המשימות הפתוחות

### 1. מיפוי 7 סיווגים חסרים
בקובץ `classifications.csv` (פורמט: `מקור,יעד`) חסרים הערכים הבאים,
שהופיעו במסמכים ולא תורגמו:
`משאיות`, `אוטובוסים`, `נגררים`, `נתמכים`, `נתמך`, `נגררים/נתמכים`, `חשמלי`

הבעיה: המסמכים לא מציינים משקל, ולכן לא ברור לאיזו תת-קטגוריה למפות.
הצעה שקיבלתי: `משאיות → N2, N3` · `אוטובוסים → M2, M3` · `נגררים/נתמכים → O1, O2, O3, O4`.
צריך להחליט ולעדכן את הקובץ.

### 2. איחוד כפילויות בשמות תוצרים (המשימה העיקרית)
בגיליון `תוצרים-DISTINCT` יש שמות תוצר כפולים שצריך לאחד לפני טעינה לטבלת REFERENCE:
- **שגיאות כתיב:** קאדילק / קאדילאק / קדילאק · קרייזלר / קרייזךר · ניסן / ניסאן · טויוטה / טיוטה · לנד רובר / לנדרובר · מזראטי / מזארטי · קוואסאקי / קוואסקי · דאצ'יה / דצ'יה · אינפיניטי / אינפניטי
- **עברית מול אנגלית:** בומברדייה / BRP · מאן / MAN · אופל / OPEL · איווקו / IVECO · פולאריס / POLARIS · דוקאטי / DUCATI · סי.אף מוטו / CFMOTO
- **קיצורים:** מרצדס / מרצדס בנץ

**מה אני רוצה שתעשה:**
1. תעבור על גיליון `תוצרים-DISTINCT` המצורף ותכין טבלת מיפוי מלאה
   (עמודות: שם קנוני, וריאנט, מספר מופעים, רמת ודאות, נימוק) — לאישור שלי.
2. אחרי שאאשר — תוסיף לסקריפט תמיכה בקובץ חדש `product_aliases.csv`
   שיעבוד בדיוק כמו `classifications.csv` (קובץ חיצוני, בלי לשנות קוד בכל עדכון),
   ויאחד את שמות התוצר לשם קנוני.

**אזהרה קריטית — מותגים שאסור לאחד** למרות שיש להם אותו יצרן:
מיני ≠ ב.מ.וו · לקסוס ≠ טויוטה · ג'נסיס ≠ יונדאי · קופרה ≠ סיאט · סמארט ≠ מרצדס · SHERCO ≠ SCORPA
(מותגים נפרדים בבעלות אותה חברת אם.)
אל תאחד אוטומטית לפי דמיון טקסט או לפי יצרן משותף — תמיד להציג לי לאישור קודם.

## איך אני רוצה שתעבוד

- תשובות קצרות ותמציתיות, בעברית.
- אל תנחש פורמטים. אם אתה לא בטוח איך משהו נראה בקובץ — תבקש ממני דוגמה.
  (בשיחה הקודמת בזבזנו הרבה זמן על ניחושים שגויים לגבי מבנה המסמכים.)
- תריץ ותבדוק כל שינוי בקוד לפני שאתה מוסר אותו, כולל בדיקת רגרסיה שלא שברת מה שעבד.
- תגיד בבירור מה נבדק על נתונים אמיתיים ומה לא.
